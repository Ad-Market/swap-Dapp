<div className="text-center font-bold text-xl text-green-400">Swap Dapp</div>
<div className="flex justify-between py-3 px-1">
    {isConnected ? (
    <div className="bg-slate-700 grid grid-cols-3 items-center rounded-lg">
        <div
            className="bg-slate-500 col-span-1 text-xs p-1 h-full font-semibold text-white text-center pt-1 rounded-l-lg"
        >
            {balance}
        </div>
        <button
            className="bg-slate-900 font-semibold text-sm col-span-2 text-white h-full p-1 w-full rounded-r-lg"
            onClick="{async"
            function
            ()
            {
            connect(false)
            }}
        >
            {account.slice(0, 6)}...{account.slice(account.length - 4)}
        </button>
    </div>
    ) : (
    <button
        className="hover:bg-slate-200 font-semibold text-sm text-white  rounded-lg"
        onClick="{async"
        function
        ()
        {
        connectFunc()
        }}
    >
        Connect
    </button>
    )}

    <button
        className="bg-slate-900 hover:bg-slate-200 font-semibold p-1 text-white rounded"
        onClick="{async"
        function
        ()
        {}}
    >
        <p className="text-sm">set slippage</p>
    </button>
</div>
